import type { FC, ReactNode } from "react";
import Head from "next/head";
import { Grid } from "@mantine/core";

type Props = {
  title: string;
  children: ReactNode;
};

export const Layout: FC<Props> = ({ children, title = "T3 Stack" }) => {
  return (
    <>
      <Head>
        <title>{title}</title>
        <meta name="description" content="Generated by create-t3-app" />
      </Head>
      <main className="mx-auto flex min-h-screen flex-col justify-center bg-gray-100">
        <Grid justify="center">
          <Grid.Col xs={8} sm={6} md={5} xl={4}>
            {children}
          </Grid.Col>
        </Grid>
      </main>
    </>
  );
};
